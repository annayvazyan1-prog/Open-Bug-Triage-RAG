# Streamlit Community Cloud installs from this file.
# (Local dev can use `uv sync` / pyproject.toml instead.)
langchain-openai>=0.1.7
langchain-core>=0.2.0
langchain-community>=0.2.0
chromadb>=0.5.0
python-dotenv>=1.0.0
streamlit>=1.36
